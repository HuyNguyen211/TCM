# Base URL of the backend API.
# Local dev: leave as-is (Vite proxies /api -> http://localhost:4000).
# Production (Vercel): set to your deployed backend, e.g. https://tcm-backend.onrender.com/api
VITE_API_URL=/api
